//! TDP Display — Ternary Display Protocol
//! Framebuffer management with phase-encryption support

#![allow(unused)]

pub struct Framebuffer {
    pub width: u32,
    pub height: u32,
    pub bpp: u32,
}
