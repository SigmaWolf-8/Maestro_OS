//! Theme Engine
//! Dynamic theming with Quantum Obsidian default

#![allow(unused)]

pub struct Theme {
    pub name: &'static str,
    pub primary_color: u32,
    pub background_color: u32,
    pub surface_color: u32,
    pub text_color: u32,
}
